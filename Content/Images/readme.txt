This folder should contain all the images that are used in the project.

If you need to, categorize the images into sub-folders (e.g. all face props into "face-props/")

Make sure to set the image properties to "Copy if newer" rather than "Do not copy"
